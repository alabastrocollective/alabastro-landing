---
const additionals = [
  { name: 'Instrumento MIDI', price: '10' },
  { name: 'Instrumento de 1 micrófono', price: '20' },
  { name: 'Instrumento multimicrófono', price: 'desde 35' },
  { name: 'Coral', price: '80' },
  { name: 'Voz', price: '15' }
];
---

<section class="additional section">
  <div class="container">
    <h2 class="section-title">Servicios Adicionales</h2>
    <p class="additional-intro">
      Cualquier servicio no incluido en el paquete contratado será considerado servicio adicional con costo independiente.
    </p>
    
    <div class="additional-grid">
      {additionals.map(item => (
        <div class="additional-item">
          <span class="additional-name">{item.name}</span>
          <span class="additional-price">${item.price}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .additional {
    background-color: var(--color-taupe);
    color: var(--color-white);
  }

  .section-title {
    text-align: center;
    font-size: 2.5rem;
    color: var(--color-white);
    margin-bottom: 1rem;
  }

  .additional-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 3rem;
    color: var(--color-white);
    opacity: 0.95;
    font-size: 1.1rem;
  }

  .additional-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .additional-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    background-color: rgba(255, 255, 255, 0.15);
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 0.75rem;
    transition: all 0.3s ease;
  }

  .additional-item:hover {
    background-color: var(--color-primary);
    transform: translateX(8px);
  }

  .additional-name {
    font-weight: 500;
    color: var(--color-white);
  }

  .additional-price {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-white);
  }

  @media (max-width: 768px) {
    .additional-grid {
      grid-template-columns: 1fr;
    }
  }
</style>