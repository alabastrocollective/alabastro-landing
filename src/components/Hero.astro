---
---

<section class="hero" id="inicio">
  <div class="hero-overlay"></div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-badge">Creative Worship Studio</div>
      <h1 class="hero-title">
        Cada canción nace con un<br>
        <span class="highlight">propósito</span>
      </h1>
      <p class="hero-description">
        Acompañamos a artistas y grupos cristianos desde la idea inicial hasta una producción profesional que refleje excelencia y fe.
      </p>
      <div class="hero-cta">
        <a href="#paquetes" class="btn btn-primary">Ver Paquetes</a>
        <a href="#contacto" class="btn btn-secondary">Contactar</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    background-image: url('/herobanner.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    overflow: hidden;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to right,
      rgba(239, 232, 224, 0.95) 0%,
      rgba(239, 232, 224, 0.85) 50%,
      rgba(239, 232, 224, 0.6) 100%
    );
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 800px;
    padding: 2rem 0;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1.5rem;
    background-color: transparent;
    border: 1px solid var(--color-primary);
    border-radius: 2rem;
    color: var(--color-text-light);
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 2rem;
    letter-spacing: 0.05em;
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    color: var(--color-text-dark);
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .highlight {
    color: var(--color-primary);
    font-style: italic;
  }

  .hero-description {
    font-size: 1.25rem;
    color: var(--color-text-light);
    margin-bottom: 2.5rem;
    max-width: 600px;
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 80vh;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-description {
      font-size: 1.1rem;
    }

    .hero-cta {
      flex-direction: column;
    }

    .hero-cta .btn {
      width: 100%;
    }
  }
</style>