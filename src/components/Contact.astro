---
---

<section class="contact section" id="contacto">
  <div class="container">
    <div class="contact-content">
      <div class="contact-info">
        <h2 class="section-title">Contáctanos</h2>
        <p class="contact-description">
          ¿Listo para dar vida a tu canción? Estamos aquí para acompañarte en cada paso del proceso creativo.
        </p>
        
        <div class="contact-methods">
          <a href="tel:+51915131933" class="contact-method">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
            </svg>
            <div>
              <span class="method-label">Teléfono</span>
              <span class="method-value"><span class="flag">🇵🇪</span> +51 915 131 933</span>
            </div>
          </a>

          <a href="tel:+584123907493" class="contact-method">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
            </svg>
            <div>
              <span class="method-label">Teléfono</span>
              <span class="method-value"><span class="flag">🇻🇪</span> +58 412 3907493</span>
            </div>
          </a>

          <a href="mailto:info@alabastrocollective.com" class="contact-method">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
              <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
            <div>
              <span class="method-label">Email</span>
              <span class="method-value">info@alabastrocollective.com</span>
            </div>
          </a>
        </div>

        <div class="contact-tagline">
          <p>"Gracias por confiar en nosotros para dar forma a lo que Dios ha puesto en tu corazón."</p>
        </div>
      </div>

      <div class="contact-form-container">
        <form class="contact-form" id="contactForm">
          <div class="form-group">
            <label for="name">Nombre completo</label>
            <input type="text" id="name" name="name" required>
          </div>

          <div class="form-group">
            <label for="email">Correo electrónico</label>
            <input type="email" id="email" name="email" required>
          </div>

          <div class="form-group">
            <label for="phone">Teléfono</label>
            <input type="tel" id="phone" name="phone" required>
          </div>

          <div class="form-group">
            <label for="package">Paquete de interés</label>
            <select id="package" name="package" required>
              <option value="">Selecciona un paquete</option>
              <option value="instrumental">Instrumental - $100</option>
              <option value="standard">Standard - $150</option>
              <option value="premium">Premium - $350</option>
            </select>
          </div>

          <div class="form-group">
            <label for="message">Cuéntanos sobre tu proyecto</label>
            <textarea id="message" name="message" rows="5" required></textarea>
          </div>

          <button type="submit" class="btn btn-primary btn-submit">Enviar consulta</button>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background-color: var(--color-background);
    color: var(--color-text-dark);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .section-title {
    font-size: 2.5rem;
    color: var(--color-text-dark);
    margin-bottom: 1rem;
  }

  .contact-description {
    font-size: 1.1rem;
    color: var(--color-text-light);
    margin-bottom: 3rem;
    line-height: 1.7;
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .contact-method {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem;
    background-color: var(--color-white);
    border-radius: 0.75rem;
    border: 1px solid var(--color-primary);
    transition: all 0.3s ease;
  }

  .contact-method:hover {
    background-color: var(--color-primary);
    transform: translateX(8px);
  }

  .contact-method:hover svg,
  .contact-method:hover .method-label,
  .contact-method:hover .method-value {
    color: var(--color-white);
  }

  .contact-method svg {
    color: var(--color-primary);
    flex-shrink: 0;
    transition: color 0.3s ease;
  }

  .contact-method div {
    display: flex;
    flex-direction: column;
  }

  .method-label {
    font-size: 0.85rem;
    color: var(--color-text-light);
    margin-bottom: 0.25rem;
    transition: color 0.3s ease;
  }

  .method-value {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-text-dark);
    transition: color 0.3s ease;
  }

  .flag {
    margin-right: 0.25rem;
  }

  .contact-tagline {
    padding: 1.5rem;
    background-color: var(--color-primary);
    border-left: 3px solid var(--color-white);
    border-radius: 0.75rem;
  }

  .contact-tagline p {
    color: var(--color-white);
    font-style: italic;
    line-height: 1.6;
  }

  .contact-form-container {
    background-color: var(--color-white);
    padding: 2.5rem;
    border-radius: 1.5rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    font-weight: 600;
    color: var(--color-text-dark);
    margin-bottom: 0.5rem;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.875rem;
    border: 2px solid #E0E0E0;
    border-radius: 0.5rem;
    font-family: inherit;
    font-size: 1rem;
    transition: border-color 0.3s ease;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
  }

  .form-group textarea {
    resize: vertical;
  }

  .btn-submit {
    width: 100%;
    padding: 1.125rem;
    font-size: 1.1rem;
  }

  @media (max-width: 968px) {
    .contact-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }
</style>

<script>
  const form = document.getElementById('contactForm') as HTMLFormElement;
  
  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    
    const formData = new FormData(form);
    const data = Object.fromEntries(formData);
    
    // Aquí puedes integrar con tu servicio de email preferido
    console.log('Form data:', data);
    
    alert('¡Gracias por tu interés! Te contactaremos pronto.');
    form.reset();
  });
</script>